name: Review DOG

on: [workflow_call]

# on:
#   push:
#     tags:
#       - v*
#     branches:
#       - '**'
#   pull_request:
#     branches:
#       - '**'

jobs:
  codeql:
    uses: ./.github/workflows/3.codeql.yaml

  golangci-lint:
    needs: [codeql]
    name: runner
    runs-on: ubuntu-latest
    steps:
      - name: Check out code into the Go module directory
        uses: actions/checkout@v3

      - name: golangci-lint
        uses: reviewdog/action-golangci-lint@v2

